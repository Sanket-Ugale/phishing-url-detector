{
  "version": 2,
  "builds": [
    {
      "src": "app/app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app/app.py"
    }
  ],
  "buildCommand": "pip install -r requirements.txt",
  "env": {
    "FLASK_ENV": "production"
  }
}
